<can-component tag="lbh3-header">
  <style type="less">
    display: block;

    p { font-weight: bold; }
  </style>
  <view>
    <p>{{message}}</p>
  </view>
  <script type="view-model">
    import DefineMap from 'can-define/map/';

    export default DefineMap.extend({
      message: {
        value: 'This is the lbh3-header component'
      }
    });
  </script>
</can-component>
